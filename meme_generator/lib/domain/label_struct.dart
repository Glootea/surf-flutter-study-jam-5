import 'package:flutter/material.dart';

class LabelStruct {
  int id;
  String text;
  Offset offset;
  Color color;
  double scale;
  LabelStruct(this.id, this.text, this.offset, this.color, this.scale);
}
